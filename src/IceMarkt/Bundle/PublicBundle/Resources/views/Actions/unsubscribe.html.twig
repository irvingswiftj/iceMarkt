<html>
    <head>

    </head>
    <body>
        {% if success %}
            <p>{{ email }} removed.</p>
        {% else %}
            <p>{{ email }} could not be found.</p>
        {%  endif %}
    </body>
</html>
